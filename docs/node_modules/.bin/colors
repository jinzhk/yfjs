#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../colors-cli/bin/colors" "$@"
  ret=$?
else 
  node  "$basedir/../colors-cli/bin/colors" "$@"
  ret=$?
fi
exit $ret
